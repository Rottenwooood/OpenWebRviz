技术栈：
    Web：React + ShadcnUI
    后端：Hono + Bun + Drizzle + Better-AuthAppFlutter + Signals + Flutter Hooks + fquery
    ROS相关： roslibjs，ros2djs
开发阶段：
    阶段一：能在本地运行，接收仿真环境提供的数据并渲染
    阶段二：暂未决定

- 每次添加功能或修复问题后 git commit
- 一次只聚焦于一个阶段，必须在前一个阶段充分测试之后才能去开发下一个阶段
- 每一个功能开发完毕后需进行测试，写测试代码或者主动停止让人来测试
- 每个功能最多只能写一个文档，写在 doc/ ，禁止一个功能同时出现“快速入门，使用指南，修改说明三个独立的文件”

需接收数据：
- /scan
- /map，/move_base/global_costmap/costmap
- /tf, /tf_static
- 路径数据 (/move_base/NavfnROS/plan (全局) 和 /move_base/TCPlanner/local_plan (局部)）)

渲染：
- 只需要2d，不需要3d
- 能够勾选某个topic是否渲染

其他：
- refer/ 中是 rviz 在ros2的官方实现，相关数据处理和渲染的细节可以参考
- doc/ 中放的是你写的文档和我写的约束文件